# ⚠️ 协作规范

> 一句话原则：**自己的任务在自己的分支上做；牵涉到别人负责的内容，先提醒、得到确认后才能动，动手前一个字都不要改。**

## 1. 分支与任务

- 开工第一件事：确认自己的分支。命名格式 `<你的名字>-<任务>`，例如 `xiaoyu-novel`、`aming-index`：
  ```bash
  git checkout main && git pull
  git checkout -b 你的名字-任务
  ```
- 每个人只在**自己的分支**上推进自己的任务；main 分支的改动一律通过 Pull Request 合并进入。
- 两条红线：合并前先 `git pull`；**永远不 force push**。

## 2. ⚠️ 跨区修改提醒（最重要的规则）

如果你的任务牵涉到**别人负责的内容**（别人写的段落、别人维护的文件）：

1. **先别改。** 哪怕只改一个字，也等确认。在自己的分支上也不要先改了"待会儿再问"。
2. **立刻发提醒，两个渠道都做**（双保险，防止漏看）：
   - 在 [NOTICES.md](NOTICES.md) 登记一行（模板在文件里）；
   - 在群里 @ 对方，消息带前缀 **`⚠️ [跨区修改申请]`**，写清楚：改什么、为什么、影响哪里。
3. **对方明确确认后才能动。** 没回复之前，你任务的其他部分照常推进，只是不碰那块内容。
4. 超过 **24 小时**没回复：群里 @全体 升级提醒；再没回应就小会上当面说定，不要擅自先改。

## 3. 提交与合并

- 小步提交，commit message 写清"改了什么、为什么"。
- 被批准的跨区修改，走 PR 合并，**PR 标题加 `⚠️` 前缀**，让 review 的人一眼看到这动了别人的地盘。
- 推送被拒（`fetch first`）说明有人比你先推了：`git fetch` 后看看远端新提交是什么，再决定怎么合并，**不要硬推**。

## 4. 出现分歧时

同一处内容两个人想改的方向不一样（例如剧情设定、页面交互），先在群里对齐"到底要哪个"，再由**一个人**落笔。两个 AI 会话 / 两个人同时各写各的版本，就会出现互相覆盖。
